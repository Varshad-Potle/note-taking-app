# 📝 Note Taking App (MERN Stack)

A secure and responsive **Note Taking Application** built using the **MERN stack** — **MongoDB**, **Express.js**, **React**, and **Node.js** — with **JWT authentication** (using salt and pepper for enhanced password security).  
The app allows users to **create, read, update, and delete** personal notes with a clean UI and token-based access control.

---

## 🚀 Features

- 🔐 **JWT Authentication** with salt & pepper hashing for advanced security  
- 🗒️ **CRUD Operations** — Create, Read, Update, and Delete notes  
- ⚛️ **React Frontend** with responsive UI  
- 🧭 **Express Routing** for modular backend APIs  
- 📦 **MongoDB Integration** for persistent data storage  
- 🔄 **State Management** using React Hooks / Context API  
- 🧠 **Protected Routes** — accessible only after authentication  
- ☁️ **RESTful API Architecture**  

---

## 🧰 Tech Stack

**Frontend:** React, Axios, TailwindCSS / Bootstrap (optional)  
**Backend:** Node.js, Express.js  
**Database:** MongoDB (via Mongoose ODM)  
**Authentication:** JWT (JSON Web Token), bcrypt with salt & pepper  
**Environment:** dotenv, cors, nodemon

### 🧩 Prerequisites

Make sure you have the following installed on your system:

- **Node.js** (v16 or later) → [Download here](https://nodejs.org/)
- **npm** (comes with Node.js)
- **MongoDB** (local installation or [MongoDB Atlas](https://www.mongodb.com/cloud/atlas))
- **Git** (for cloning the repository)

To check if they’re installed:
```bash
node -v
npm -v
git --version


### 1️⃣ Clone the Repository
```bash
git clone https://github.com/<Varshad-Potle>/note-taking-app.git
cd note-taking-app
